# `script.sh`

Given ur in CLion, opens the code at ur cursor position in the chromium code browser, `https://cs.chromium.org/`.

# `codeSearch2CLion.sh`

Given ur in the online code browser, opens the code at ur cursor position in CLion.
